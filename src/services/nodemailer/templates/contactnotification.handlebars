<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="color-scheme" content="light only" />
    <title>New contact message</title>
  </head>
  <body style="margin:0; padding:0; background:#f5f7fa; font-family:Arial, Helvetica, sans-serif;">
    <div style="display:none; max-height:0; overflow:hidden;">
      New message received via the website contact form.
    </div>
    <table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="background:#f5f7fa; padding:32px 16px;">
      <tr>
        <td align="center">
          <table role="presentation" width="560" cellpadding="0" cellspacing="0" style="max-width:560px; width:100%; background:#ffffff; border-radius:16px; overflow:hidden;">
            <tr>
              <td style="background:linear-gradient(135deg, #0FA53A, #0FF338); height:6px; font-size:0; line-height:0;">&nbsp;</td>
            </tr>
            <tr>
              <td style="padding:36px 40px 8px 40px;">
                <h1 style="margin:0; font-size:20px; color:#0D1F15;">New message for {{companyName}}</h1>
              </td>
            </tr>
            <tr>
              <td style="padding:12px 40px 0 40px;">
                <table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="font-size:14px; color:#4C5B51;">
                  <tr>
                    <td style="padding:6px 0; width:160px; font-weight:bold; color:#0D1F15;">Name</td>
                    <td style="padding:6px 0;">{{name}}</td>
                  </tr>
                  <tr>
                    <td style="padding:6px 0; font-weight:bold; color:#0D1F15;">Email</td>
                    <td style="padding:6px 0;">{{email}}</td>
                  </tr>
                  {{#if phone}}
                  <tr>
                    <td style="padding:6px 0; font-weight:bold; color:#0D1F15;">Phone</td>
                    <td style="padding:6px 0;">{{phone}}</td>
                  </tr>
                  {{/if}}
                  {{#if subject}}
                  <tr>
                    <td style="padding:6px 0; font-weight:bold; color:#0D1F15;">Subject</td>
                    <td style="padding:6px 0;">{{subject}}</td>
                  </tr>
                  {{/if}}
                  {{#each rows}}
                  <tr>
                    <td style="padding:6px 0; font-weight:bold; color:#0D1F15;">{{this.label}}</td>
                    <td style="padding:6px 0;">{{this.value}}</td>
                  </tr>
                  {{/each}}
                </table>
              </td>
            </tr>
            <tr>
              <td style="padding:20px 40px 32px 40px;">
                <div style="background:#F7FAF7; border:1px solid #E8ECE8; border-radius:10px; padding:16px 18px; font-size:14px; line-height:1.7; color:#0D1F15; white-space:pre-line;">{{message}}</div>
              </td>
            </tr>
            <tr>
              <td style="padding:0 40px 32px 40px; font-size:12px; color:#7A877E;">
                Reply directly to {{email}} to answer this message.
              </td>
            </tr>
          </table>
        </td>
      </tr>
    </table>
  </body>
</html>
